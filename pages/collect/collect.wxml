<view class="contain">
  <view class="artical" wx:for="{{collect}}" wx:key="key" wx:for-item="item">
    <image class="aimg" src="{{item.story.img}}"></image>
    <view class="desc">
      <view class="title">{{item.story.storyName}}</view>
      <text class="bookmark fa fa-bookmark"> 正篇：13%</text>
    </view>
  </view>

</view>